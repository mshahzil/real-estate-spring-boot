spring.datasource.url=jdbc:mysql://s9-db.chjhkjc05wre.us-east-1.rds.amazonaws.com:3306/s9_db
spring.datasource.username=root
spring.datasource.password=rootpass
#spring.datasource.driver-class-name =com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql= true

# ============ MULTIPART ==================
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=50MB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=200MB
spring.http.multipart.max-file-size=200MB
spring.http.multipart.max-request-size=200MB
upload_max_filesize=200M
post_max_size=200M


#Define a custom port instead of the default 8080
#server.port=8443
#
# Tell Spring Security (if used) to require requests over HTTPS
#security.require-ssl=true
#
## The format used for the keystore
#server.ssl.key-store-type=PKCS12
## The path to the keystore containing the certificate
#server.ssl.key-store=classpath:keystore.p12
## The password used to generate the certificate
#server.ssl.key-store-password= passwords9
## The alias mapped to the certificate
#server.ssl.key-alias=s9